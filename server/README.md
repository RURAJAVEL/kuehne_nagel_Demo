# Sample Application Server repo 

Start the server.
```
KUEHNE_NAGEL_DEMO/server> npm install
KUEHNE_NAGEL_DEMO/server> npm run dev
```

Build Docker Image 
```
docker build -t demopods-eks-image .
```